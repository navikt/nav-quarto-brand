[project]
name = "nav-quarto-brand"
version = "0.1.0"
description = "Quarto brand extension for Nav"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "prek>=0.3.9",
    "quarto-cli>=1.8.27",
]

[tool.uv]
exclude-newer = "1 week"
